[pytest]
testpaths = tests
# The scripts import each other by bare name (`from common import ...`), which
# works when they're run directly because that puts scripts/ on the path. This
# does the same for the tests. Deliberately not a pyproject.toml: this repo is
# a set of scripts, not a package, and nothing here is meant to be installed.
pythonpath = scripts
